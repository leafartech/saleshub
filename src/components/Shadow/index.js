import styles from './Shadow.module.css'

export default function Shadows() {
    return (
        <div className={ styles.shadows }>
            {/* CONFIGURAR "SOMBRAS" para aparecerem enquanto a página carrega*/}
        </div>
    )
}