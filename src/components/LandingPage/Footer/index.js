import styles from './Footer.module.css'

export default function Footer({}) {
    return (
        <footer className={ styles.rodape }>
            <p>Desenvolvido por Rafael Borges Bezerra</p>
        </footer>
    )
}